Aufgabe:

Erstelle ein Eingabe-Feld, in das ein Farbwert eingetragen werden kann (wer will kann auch noch zusätzlich einen geeigneten Color-Picker einsetzen)
Erstelle einen Button mit einen Klick-Event-Listener
Auf das Klick-Event soll der Farbwert in den local Storage geschrieben werden
Erstelle einen weitere Seite
Registriere auf dieser Seite einen Event-Listener für den local Storage.
Wenn das Event gefeuert wird soll der Farbwert aus dem Storage gelesen und als background-color im Styling des Bodies eingesetzt werden.
